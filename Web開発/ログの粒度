# Summary
エラーの粒度は大きく分けて、4つ。(Debug, Info, Warning, Error)

## Detail
1. Debug    
開発環境のみで使われる。    
サーバスタート時や、ログのインスタンス生成時に出力(割と、ユースケースがはっきりしていない)    

2. Info   
開発環境のみで使われる。    
処理内容を出力する。(関数を呼び出すタイミングで出力した方が良い？ログの数エグくならないか？)

3. Warning
動作に問題はないが、気にはすべきレベルのログ。   
CLIだと、依存ライブラリのバージョンが古い時とかに出される。

4. Error
動作に問題あるエラー
